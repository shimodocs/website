{
  "name": "shimodocs-site",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && vite build --ssr src/entry-server.jsx --outDir dist-ssr && node scripts/prerender.mjs",
    "build:client": "vite build",
    "build:seo": "vite build --ssr src/entry-server.jsx --outDir dist-ssr && node scripts/prerender.mjs",
    "assets": "python3 scripts/make-assets.py",
    "preview": "vite preview --port 4173"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "react": "latest",
    "react-dom": "latest",
    "react-router": "latest",
    "react-router-dom": "latest",
    "vite": "latest"
  },
  "devDependencies": {}
}
