node_modules
dist
dist-ssr
.DS_Store
.env
.env.*
!.env.example
__pycache__

# SEO working material: competitive teardowns, keyword gaps, the link-building
# targets and the campaign roadmap. This repository is public and none of it is
# read by the build, so it is kept out of the tree deliberately rather than by
# omission. Nothing here is referenced by src/, scripts/ or the workflows.
seo/

# Overseas GTM working material: customer archaeology, pipeline, ICP,
# commercial operating model. Same reason as seo/: public repo, not
# read by the build, may hold customer and campaign data.
overseas/
