name: Build ShimoDocs website

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          # The sitemaps date every URL from the commit that last changed its
          # content, so the build needs the history. A shallow clone collapses
          # those dates onto one day and the prerenderer fails the build over it.
          fetch-depth: 0
      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
      - name: Install dependencies
        run: npm ci
      # The build prerenders every route and fails if a page would ship without
      # a unique title, canonical link or structured data.
      - name: Build and prerender
        env:
          VITE_SITE_URL: ${{ vars.SITE_URL || 'https://shimodocs.com' }}
        run: npm run build
      - name: Report crawler surfaces
        run: |
          echo "--- routes ---"
          find dist -name index.html | sort
          echo "--- robots.txt ---"
          cat dist/robots.txt
          echo "--- sitemap.xml urls ---"
          grep -c '<loc>' dist/sitemap.xml
